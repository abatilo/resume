apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resumeChart.fullname" . }}-test-connection"
  labels:
{{ include "resumeChart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "resumeChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
